import { MemoryCenter } from "@/features/memory/memory-center";

/**
 * Capabilities → Memory entry.
 * Product surface is the six-tab Memory Center (learning closed loop).
 */
export function MemoryCapabilitiesPanel() {
  return <MemoryCenter />;
}
